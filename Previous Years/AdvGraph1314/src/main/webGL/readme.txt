To get around cross-site security restrictions, you can't load these files in a local web browser--you have to load them over HTTP (but it's fine if that's from a local server.)  

Any lightweight HTTP server will do.